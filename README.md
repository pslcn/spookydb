# ukeyvalue

### Usage

```bash
# Get value in 'akey'
curl localhost:8080/akey

# Put 'avalue' in 'akey'
curl -X PUT localhost:8080/akey -d avalue

# Delete key 'akey'
curl -X DELETE localhost:8080/akey
```
